import { Url2TitlePipe } from './url-2-title.pipe';

describe('Url2TitlePipe', () => {
  it('create an instance', () => {
    const pipe = new Url2TitlePipe();
    expect(pipe).toBeTruthy();
  });
});
